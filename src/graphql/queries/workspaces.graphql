query workspaces {
	workspaces(locales: pt_BR) {
		company
		role
		startedIn
		endIn
		description {
			html
		}
		activeJob
		companyProjects(locales: pt_BR) {
			name
			url

			description
			techs
		}
	}
}
