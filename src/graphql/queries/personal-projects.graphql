query PersonalProjects {
	personalProjects(orderBy: releaseDate_DESC, locales: pt_BR) {
		id
		name
		description
		releaseDate
		github
		demoUrl
		videoDemo {
			url
		}
		features {
			name
			description
		}
		images {
			url
		}
		thumbnail {
			url
		}
	}
}
